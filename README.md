# final_task_7
# 🛠 Преобразование императивного кода в функциональный стиль

## 📝 Описание проекта

Данный проект демонстрирует рефакторинг кода с **императивного** на **функциональный** стиль программирования на Scala. В качестве примера взята простая задача обработки списка строк.

## 🎯 Исходная задача

Изначально код выполняет следующие операции над списком строк:
- 📋 Принимает список строк
- 🔍 Фильтрует строки, оставляя только те, длина которых больше 3 символов
- 🔠 Преобразует отфильтрованные строки в верхний регистр
- 📤 Возвращает новый список с обработанными строками

## ❌ Исходный код (императивный стиль)

```scala
object StringProcessor {
  def processStrings(strings: List[String]): List[String] = {
    var result = List[String]()        // ❌ Изменяемая переменная
    for (str <- strings) {              // ❌ Цикл for
      if (str.length > 3) {              // ❌ Императивная проверка
        result = result :+ str.toUpperCase  // ❌ Мутация списка
      }
    }
    result
  }

  def main(args: Array[String]): Unit = {
    val strings = List("apple", "cat", "banana", "dog", "elephant")
    val processedStrings = processStrings(strings)
    println(s"Processed strings: $processedStrings")
  }
}
✅ Исп
